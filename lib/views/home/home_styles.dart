/// Radii and spacing shared by the home tab's sections. Kept together so the
/// hero, cards and tiles read as one surface rather than five widgets each
/// picking their own corner.
const kHomeHeroRadius = 24.0;
const kHomeCardRadius = 20.0;
const kHomeTileRadius = 16.0;
const kHomeSectionGap = 28.0;
